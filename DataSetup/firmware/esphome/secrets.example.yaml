# Copy to secrets.yaml (same folder) and fill in. secrets.yaml is git-ignored.
wifi_ssid: "your-ssid"
wifi_password: "your-wifi-password"
api_encryption_key: "generate-with: openssl rand -base64 32"
ota_password: "choose-an-ota-password"
